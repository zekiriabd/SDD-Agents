"""sdda_scripts — validateurs et compilateurs déterministes (0 token) de SDD_Agents.

Chaque script est exécutable en CLI (`python sdda_scripts/<nom>.py`), accepte
`--json` pour une sortie machine et renvoie 0 (vert) ou 1 (rouge).
"""
