# SDD_Agents — secrets et artefacts générés
# STACK.md est VERSIONNÉ (il ne porte que des noms de variables) ; les valeurs
# vivent dans .env. src/ est le livrable : il se versionne comme tout code.
workspace/traces/
workspace/.sys/
workspace/evals/reports/
/.env
workspace/**/.env
__pycache__/
*.pyc
.pytest_cache/
.venv/
